# 星漫匣 / 樱漫盒 clean —— 项目交接手册

> 最后更新：2026-08-10 ｜ 维护者：独立开发者（一人项目）
> 配套机器记忆：`.workbuddy/memory/MEMORY.md`（AI 助手自动读取）

---

## 0. 一句话定位
多源聚合的「**漫画 + 动漫（视频）+ 小说**」阅读/追番 App，**Flutter（Dart）** 实现。
是对原「樱漫盒」APK（v1.08，Kotlin + 大量广告 SDK + TTEncrypt 加壳）的**去广告干净重写版**。

原 APK 的逆向解剖报告在 `../analysis/REPORT.md`（协议/加密细节的取证来源，接手前建议扫一遍）。

---

## 1. 技术栈
- 语言：Dart（Flutter 3.x）
- 规模：**54 个 Dart 文件 / 约 16,500 行**
- 关键依赖（pubspec.yaml）：
  - **网络**：零三方依赖，自写 `Net`（`lib/net/http_client.dart`，基于 `dart:io`）
  - **加密**：`crypto` + `pointycastle`（AES / HMAC / MD5）
  - **图片**：`image`（JM 解扰、工具箱图片处理）
  - **WebView**：`webview_flutter`（NekoGAL 入口、登录页）
  - **播放**：`media_kit` + `media_kit_video` + `media_kit_libs_video`（动漫/视频播放）
  - **存储**：`path_provider`
  - **UI 工具**：`screen_brightness` / `volume_controller`

---

## 2. 环境与构建
> ⚠️ **本 WorkBuddy 沙箱内 `flutter` 不在 PATH**。构建需在装有 Flutter SDK 的机器上进行（此前 APK 是在另一套环境里 `flutter build apk` 产出的）。

构建命令：
```bash
cd clean_manhua_flutter
flutter pub get
flutter build apk --debug          # 产物：build/app/outputs/flutter-apk/app-debug.apk
# 或 flutter run 直连设备/模拟器
```
当前已产出：`app-debug.apk`（2026-08-08，约 122MB）。

> ⚠️ **项目无 git 版本控制**。交接后第一件事建议初始化 git，把 `clean_manhua_flutter/` 单独作为仓库（根目录 `back/` 有大量逆向/探测临时产物，**不要**一起进版本库）。

---

## 3. 代码地图（lib/）
```
lib/
├── main.dart                      # 入口：绑定各 Store，注册源
├── theme.dart                     # 主题（粉色系 anime 风）
├── models/comic_item.dart         # 通用数据模型
├── net/
│   ├── http_client.dart           # Net：零三方 HTTP 客户端 + Cloudflare 优选 IP
│   ├── aes_cbc.dart               # AES 工具（豆包章节图解密等）
│   ├── jm_crypto.dart / jm_scramble.dart  # 禁漫解密 + 图片解扰
│   ├── local_store.dart           # 通用 JSON/文件持久化（底层）
│   ├── bookshelf_store.dart       # 漫画书架
│   ├── novel_shelf_store.dart     # 小说书架（独立文件）
│   ├── download_manager.dart      # 下载管理
│   ├── image_cache.dart           # 图片缓存清理
│   ├── circuit_breaker.dart       # 源熔断（防雪崩）
│   └── route_diagnostic.dart      # 路由/连通性诊断
├── sources/
│   ├── comic_source.dart          # ComicSource 抽象
│   ├── video_source.dart          # VideoSource 抽象
│   ├── novel_source.dart          # NovelSource 抽象
│   ├── source_manager.dart        # 源注册中心（看这里了解接了哪些源）
│   ├── source_config.dart         # 声明式源配置（域名/镜像外置，免发版改域名）
│   ├── source_http.dart           # 带配置感知的 HTTP 封装
│   ├── source_result.dart         # 分页/结果模型
│   ├── baozi_source.dart          # 包子漫画（免登录直链）
│   ├── doubao_source.dart         # 豆包
│   ├── yyfun_source.dart          # 樱漫(YYFun)
│   ├── picacg_source.dart         # 哔咔（需登录，HMAC 签名）
│   ├── jm_source.dart             # 禁漫（反爬，图片加扰）
│   ├── mangadex_source.dart       # MangaDex（兜底）
│   ├── agedm_video_source.dart    # AGE 动漫
│   ├── tvtfun_video_source.dart   # TvTFun 番剧
│   ├── xifan_video_source.dart    # 稀饭动漫（JSON+HTML+mp4 直链）
│   └── biquge_novel_source.dart   # 笔趣阁小说
├── ui/
│   ├── main_shell.dart            # 底部 4 Tab：首页/书架/工具/我的
│   ├── home_page.dart             # 首页（漫画/动漫/小说 三态切换）
│   ├── detail_page.dart / reader_page.dart          # 漫画详情/阅读器
│   ├── anime_home_page.dart / anime_player_page.dart / native_player_page.dart
│   ├── novel_home_page.dart / novel_detail_page.dart / novel_reader_page.dart
│   ├── bookshelf_page.dart / profile_page.dart / settings_page.dart
│   ├── toolbox_page.dart          # 工具箱（下载管理/缓存清理/图片/文本/NekoGAL）
│   ├── webview_page.dart          # 通用 WebView（NekoGAL 入口）
│   ├── pica_login_page.dart / pica_register_page.dart
│   ├── source_manage_page.dart    # 源管理（编辑域名/镜像）
│   └── widgets/                  # cached_image / jm_scramble_image / motion / player_widgets
└── utils/anime4k.dart             # 视频超分着色器加载（assets/anime4k/*.glsl）
```

---

## 4. 架构要点（交接必读）
### 4.1 源 = 引擎代码 + 声明式配置（关键设计）
「禁漫域名一死就要改代码重发版」是原始痛点。解法是把**易变的东西（域名/镜像/请求头/登录开关）外置到 `SourceConfig`**（`lib/sources/source_config.dart`）：
- `SourceConfigStore.defaults()` 内置默认配置随包发布；
- 用户在「源管理」页改的域名会持久化到 `sources_config` JSON，按 `engineId` 覆盖；
- 预留 `remoteUrl`：未来域名过期可后台发一份 JSON **免发版**更新。
- **新增源** = 写一个实现 `ComicSource`/`VideoSource`/`NovelSource` 的类 + 在 `source_manager.dart` 注册 + 在 `source_config.dart` 加一条默认配置。

### 4.2 网络层
`Net`（`http_client.dart`）零三方依赖，关键能力：
- **Cloudflare 优选 IP**：`preferredHostIps` 给特定域名强制直连候选 IP + SNI（见 §5.3）；全部 IP 失败时回退系统 DNS，避免整源挂死。
- 自签证书兼容（`badCertificateCallback` 放行），适配部分源。

### 4.3 UI 框架
`MainShell` 底部 4 Tab（首页/书架/工具/我的）。首页内部再做 **漫画/动漫/小说 三态切换**（`main_shell.dart` 的 `_type` SegmentedControl）。设置页从底部 Tab 移到了工具箱里的齿轮入口。

### 4.4 UI 重做进度（对照 `UI_v2.html`，S1–S15）
设计规范（`lib/theme.dart`）：墨蓝 `#3A6EA5`、米白背景 `#FAFAF8`、卡片14/按钮9/输入10 圆角、Remix 线条图标。

已重做（3 屏）：**我的页 S8**（用户卡/三格统计/功能列表/夜间模式/帮助弹窗/阅读历史弹窗，`ProfilePage` 改为公开 State + `refresh()`，已接 MainShell Tab3）；**阅读器 S5/S6**（去 AppBar 全黑沉浸、顶部玻璃圆钮、底部悬浮工具栏、居中页码、点击切换浮层 3s 自动隐藏、亮度/翻页模式抽屉、章节内页目录跳页）；**书架 S7**（墨蓝 tab 下划线、最近在读卡片带进度条+百分比、收藏网格「编辑/完成」模式删除按钮+确认弹窗，`BookshelfStore.sourceIdOf()` 反查源）。

已审计未做：首页错误态缺「切换数据源」按钮；动漫首页缺三区块；详情页缺更新时间/观看人数徽章。

---

## 5. 已接入源清单（含健康度）
### 漫画（6）
| 引擎 | 名称 | 登录 | 状态 | 备注 |
|---|---|---|---|---|
| baozi | 包子漫画 | 否 | ✅ 可用 | 免登录直链 `s1/s2.bzcdn.net`；2026-08-08 端到端验证 |
| doubao | 豆包漫画 | 否 | ✅ 可用 | 章节图 AES 解密已验证 |
| yyfun | 樱漫(YYFun) | 否 | ✅ 可用 | 应用自有后端，中文内容 |
| picacg | 哔咔漫画 | 是 | ⚠️ 登录失败 | 签名已对，但测试账号被服务端拒（见 §5.2） |
| jm | 禁漫天堂 | 否 | ⚠️ 不稳定 | 反爬/验证码；图片需解扰（见 §5.1） |
| mangadex | MangaDex | 否 | ✅ 兜底 | 英文/非 R18，中文优先 + 章节过滤 |

### 视频（3）
| 引擎 | 名称 | 状态 | 备注 |
|---|---|---|---|
| agedm | AGE 动漫 | ✅ | |
| tvtfun | TvTFun | ✅（需优选 IP） | 见 §5.3 |
| xifan | 稀饭动漫 | ✅ | JSON 列表/搜索 + HTML 详情 + mp4 直链；2026-08-08 端到端播放验证 |

### 小说（1）
| 引擎 | 名称 | 状态 | 备注 |
|---|---|---|---|
| biquge | 笔趣阁 | ⚠️ 默认域名可能 404 | host 可配置，需在手机端「源管理」换可用镜像 |

---

## 5. 关键技术暗坑（踩过的雷，别再踩）
### 5.1 禁漫(JM) 图片加扰 —— `lib/net/jm_scramble.dart`
禁漫对部分专辑（aid ≥ 220980）做「**纵向分块倒序**」混淆，不是简单加密：
- `getNum(aid, filename)` 按 aid 区间决定分块数：<220980 不加密；[220980,268850) 固定 10 块；≥268850 用 `MD5(aid+文件名)` 末字符取模（2023-02-08 起取模由 10 改 8）×2+2。
- `descramble()` 把图片按高度切 num 条带、**块顺序倒转**拼回。还原失败回退原图，**绝不抛异常**（防止整页崩）。
- 参考：hect0x7/JMComic-Crawler-Python。

### 5.2 哔咔(Pica) 签名 —— `lib/sources/picacg_source.dart`
- **真实协议是 HMAC-SHA256**，不是早期开源项目用的裸 MD5（裸 MD5 被服务端静默封，返回空 success）。
- `signature = HMAC-SHA256(key=_secretKey, msg=lower(path+time+nonce+method+apiKey))`，`_secretKey` 带 `<>` 等特殊字符（见 `picacg_source.dart:24`）。
- 固定 nonce。**密钥属于敏感信息，交接时注意是否要轮换**。
- 当前测试账号 `3167245951@qq.com / li081600` 被服务端拒（400 invalid email or password）——签名本身已正确，是凭据问题，需用户在官方 App 改密码后用新凭据。

### 5.3 Cloudflare 优选 IP —— `lib/net/http_client.dart`
- 当前只给 `www.tvtfun.net` 配了优选 IP 列表（多个 Cloudflare 节点），通过 `connectionFactory` 强制直连候选 IP + SNI。
- 全部优选 IP 失败时自动回退系统 DNS。
- **若某源出现 DNS 解析失败/被墙/超时，优先在这里加优选 IP，而不是改源代码里的 URL。**
- 旧记忆里「pica 优选 IP」已失效/未配置，以当前代码为准。

### 5.4 其他源要点
- **包子漫画**：Next.js-free、无 CF、无加密；章节图直链 `s1/s2.bzcdn.net/scomic/{slug}/{section}/{section}-{xxx}/{n}.jpg`；章节 URL `/comic/chapter/{slug}/{S}_{C}.html`。无需登录，最稳的漫画源。
- **稀饭动漫**：`anime.xifanacg.com`，列表/搜索走 JSON，详情走 HTML 解析，播放是 mp4 直链（media_kit 直接播）。
- **笔趣阁小说**：经典模板（搜索→书详情→章节内容），但默认域名在部分网络 404，**domain 可配置**，手机端「源管理」里换 `xbiquge.la` / `biquge.com.tw` 等镜像即可。

---

## 6. 存储与持久化
- `LocalStore`（`local_store.dart`）：通用 JSON/文件读写，所有持久化的底层。
- 书架：`bookshelf_store.dart`（漫画）、`novel_shelf_store.dart`（小说，独立 JSON 文件）。
- 源配置：`sources_config` JSON（用户覆盖，按 engineId 合并）。
- 图片缓存：`image_cache.dart` 提供清理接口（工具箱「缓存清理」用过，约 14.6MB）。
- 下载：`download_manager.dart`（工具箱「下载管理」）。

---

## 7. 验证记录
- **2026-08-08**：模拟器（900×1600，API 32）端到端验证通过 ——
  - 稀饭动漫全链路（列表→详情→选集→media_kit 播放 1080p MP4）；
  - 包子漫画加载 48 部热榜+精选；
  - 小说模式渲染（笔趣阁默认域名 404 已优雅降级 + 重试）；
  - 工具箱 5 大板块（下载管理/缓存清理/图片工具/文本工具/NekoGAL）全部渲染。
  - App 启动无崩溃。

---

## 8. 已知问题 / 待办（按优先级）
1. **JM 源不稳定**：默认域名/反爬可能失效，需接上图片加扰并保证存活域名切换。
2. **哔咔登录**：签名已对，但测试凭据被拒，需用户提供可用账号/改密码。
3. **笔趣阁默认域名**：可能 404，源管理页换镜像。
4. **无 git**：立即初始化，避免代码丢失。
5. **JS 自定义图源引擎**：曾有两个半成品文件已删除（含 15 error、无引用）；若要做 Venera 风格自定义源，需基于 `flutter_qjs` 重新实现 + 注册 + 做自定义源 UI。
6. **动漫弹幕**：之前调研提过，代码未见实现。
7. **README/文档**：Flutter 默认模板，建议用本手册替换。
8. **CF 优选 IP**：目前仅 tvtfun 配置，其他源按需补。

---

## 9. 交接清单（接手后立即做）
- [ ] 在装有 Flutter SDK 的机器拷入 `clean_manhua_flutter/`，`flutter pub get` + `flutter build apk` 验证可构建。
- [ ] 初始化 git，把 `clean_manhua_flutter/` 作为独立仓库（排除 `back/` 根目录的临时产物）。
- [ ] 跑一遍模拟器，确认 6 漫画 / 3 视频 / 1 小说源实际可用性（沙箱有外网，可直接探活）。
- [ ] 重点核对 JM 解扰、哔咔签名、tvtfun 优选 IP 三处暗坑是否仍生效（源站可能已变）。
- [ ] 确认哔咔测试账号状态；如不可用，移除或提示用户自建账号。
- [ ] 可选：把本手册内容同步进 `README.md`。

---
配套 AI 记忆：`.workbuddy/memory/MEMORY.md`（自动读取）+ 每日日志 `2026-08-XX.md`。
