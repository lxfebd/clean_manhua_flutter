# user-guide.md — 星漫匣 功能使用说明

> 面向使用者的功能说明。按"漫画 / 动漫 / 小说 / 通用"组织，每个功能给出入口与操作方式。

---

## 1. 漫画

### 找书
- **分类/排行**：首页顶部切换分类、查看排行；`detail_page` 展示简介/章节列表。
- **搜索**：首页/统一搜索页输入关键词（`unified_search_page`），支持多源联合结果。
- **切换源**：详情页或源管理页切换数据源（动漫屋/豆包/禁漫/MangaDex），源失效时有线路诊断提示。

### 阅读器（`reader_page`）
- **翻页模式**：设置里选 **横向翻页**（左右滑页）或 **纵向滚动**（连续卷动）、**双页**（漫画对开）、**条漫**（竖版长图滚动）。
- **点击局部放大镜**：点按画面局部放大查看细节（双指缩放拖动同样可用）。
- **自动裁边去白边**：设置 → 开启"自动裁边"，自动识别并裁掉四周纯白边（四边限裁 35%，小图自动跳过）。
- **图片超分**：设置 → 超分档位（关/性能/质量），低端机自动限制。
- **书签与续读**：阅读中下拉/点按菜单可加书签；书架"历史"记录上次读到第几话第几页，支持纵向滚动精确续读。
- **下载离线**：详情页下载整话/整本，设置里可选下载画质（原图/压缩）。

## 2. 动漫（番剧）

### 找番
- 动漫首页分类/搜索/热门榜单；每个条目可看剧集列表与线路（源）。
- **线路切换**：详情/播放页可切换 AGE / TvTFun / 稀饭 / Anime1 等线路。

### 播放（双播放器自动衔接）
1. 点开某集 → 先进入**网页播放器**（`anime_player_page`）：后台 WebView 加载站点页，**解析期间静音**、显示"解析直链中…"（最多 8 秒）。
2. 捕获到真实视频直链后 → **自动切换原生播放器**（`native_player_page`）：mpv 硬解 + Anime4K 超分 + 弹幕。切换瞬间网页播放器被**物理移除**，不会双音轨。
3. 若 8 秒内未捕获直链（源改版等）→ 降级为网页播放并提示，可换线路或稍后重试。

### 原生播放器功能
- **超分画质**：设置档位（关/性能/质量），Anime4K CNN 实时提升。
- **倍速**：0.25x / 0.5x / 0.75x / 1.0x / 1.25x / 1.5x / 2.0x / 3.0x / 3.5x / 4.0x；画面长按可临时 3x 快进。
- **弹幕**：自动加载该集弹幕，可开关/调节透明度/字号。
- **全屏**：点全屏（或桌面按 F）进入横屏沉浸全屏；退出恢复。
- **自动连播**：当前集播完自动下一集（可关）。
- **观看记录**：书架"动画记录"续播，记住线路/季/集。

### 桌面快捷键（播放页）
空格 播放/暂停 · ←/→ 快退/快进 10s · M 静音 · F 全屏 · Esc 返回。

## 3. 小说

### 找书
- 小说首页分类/搜索；源：笔趣阁、新笔趣阁。
- **本地导入**（`novel_import_page`）：书架 → 小说 → 导入 → 选 **TXT** 或 **EPUB** 文件 → 自动解析章节 → 加入"本地导入"书架。导入在后台 Isolate 完成，大文件不卡 UI。

### 阅读器（`novel_reader_page`）
- **排版**：字号（14~22）、行距（1.5~2.0）、段间距（8/14/18/24/30）、首行缩进（关/2 字符）。
- **背景**：跟随 / 米白 / 浅绿 / 深青（自定义背景不含色温滤镜）。
- **色温护眼**：滑杆 0~100 无级调节暖色滤镜（0=关，100≈3000K 最暖），正文区叠加暖色半透明层。
- **TTS 朗读**：点工具栏朗读按钮 → 从当前章节/段落开始朗读；支持暂停/继续、语速/音量/音调调节、**朗读到文末自动切下一章**。切换章节/退出自动停止。
- **章节切换**：章节列表弹层；上一章/下一章按钮；桌面 PageUp/PageDown/空格 滚动。

## 4. 通用

### 书架（`bookshelf_page`）
- 分区：**漫画 / 动漫记录 / 小说 / 书签**。
- 漫画：收藏更新检查（后台轮询，有新话提醒）、搜索筛选、排序。
- 动漫记录：续播/删除记录。
- 小说：本地导入与在线小说的阅读记录。
- 书签：手动收藏的漫画页，一键跳转。
- **WebDAV 同步**：设置里配置服务器/账号/密码，可备份/恢复书架、书签与设置（跨设备）。

### 设置（`settings_page`）
- 主题（亮/暗/跟随 + 换色）、字号显示、下载设置、播放默认档位、源管理（启用/层级）、更新检查、WebDAV、数据清理。

### 网络
- **代理**：设置里配置 HTTP/Socks 代理（`Net.restoreProxy`）。
- **优选 IP**：Cloudflare 优选 IP 直连加速（断网自动重试）。
- **预下载**：Wifi 下自动预取下一话/下一集，断网仍可看。

### 响应式
- 手机：单列布局；平板：播放页横屏分栏（左视频右面板）；桌面：窗口记忆 + 键盘快捷键。